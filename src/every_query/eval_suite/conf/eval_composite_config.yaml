defaults:
  - _self_
  - eval_codes: admin_codes

# inputs
model_run_dir: /users/gbk2114/EveryQuery/results/outputs/2026-01-03/15-43-49/
task_name: readmiss_30d
task_set_dir: /users/gbk2114/eq_stuff/tasks/eval/aces/${task_name}/held_out

ckpt_path: last

query_codes: ${eval_codes}

# where to save eval outputs (outside hydra run dir if you want)
#output_root: ${model_run_dir}/eval/${index_time_hash}/
output_root: ${model_run_dir}/eval/${task_name}/
do_overwrite: true
do_resume: false
seed: 140799
only_preprocess: false
