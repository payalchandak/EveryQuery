defaults:
  - _self_
  - eval_codes: 10000_ID__8db2be6fadf8

#This defines the index times to create task dfs from
index_hash: 7573f855c4b050a9d79d57fefd8a139c

# Durations (in days) to generate eval tasks for
durations: [60, 90, 365, 731]

# Splits to generate tasks for
splits: ["held_out"]

paths:
  index_times_base: ${oc.env:TASK_DIR}/eval/index_times/${index_hash}
  task_dir_base: ${oc.env:TASK_DIR}
  out_root_dir: ${oc.env:TASK_DIR}/eval/tasks_from_index_new

skip_existing: false

# Extra codes to manually add (on top of eval_codes)
extra_codes: ["MEDS_DEATH"]
